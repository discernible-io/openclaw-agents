Managed automatically by `ensure_agent_knowledge_scope_doc` in `scripts/lib.sh`
on every bootstrap when `IDENTYCLAW_KNOWLEDGE_ENABLED=1`. Do not hand-edit the
live block — change `_knowledge_scope_agents_block()` in lib.sh instead.

The upserted section title is `## Knowledge scope (strict)` in `workspace/AGENTS.md`.
